/* ============================================================
   Pescaria — scripts base
   (placeholder — será substituído conforme o prompt do projeto)
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  console.log("🎣 Projeto Pescaria — estrutura base carregada.");
});
